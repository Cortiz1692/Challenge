server {
    listen 443 ssl;
    ssl_certificate /etc/nginx/conf.d/PNA2024.crt;
    ssl_certificate_key /etc/nginx/conf.d/pna2024.key;
    server_name inverse.proxy;

    location / {
        add_header Access-Control-Allow-Origin *;
        add_header 'Access-Control-Allow-Headers' 'Authorization,Accept,Origin,DNT,X-CustomHeader,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Content-Range,Range';
        add_header 'Access-Control-Allow-Methods' 'GET,POST,OPTIONS,PUT,DELETE,PATCH';
        add_header 'Access-Control-Max-Age' 1728000;

        # Manejo para preflight requests (OPTIONS)
        if ($request_method = 'OPTIONS') {
            return 204;
        }

        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }
       # Nueva ubicación para redirigir a la nueva dirección
    # location = / {
    #     return 302 https://sistemafirmadigitaldpsn.prefecturanaval.gob.ar/login;  # Cambia esta URL por la nueva dirección
    # }

    # # Opción para servir un mensaje de redirección
    # location = /redirect {
    #     default_type text/html;
    #     return 200 '<h1>Redirigiendo...</h1><p>Por favor, acceda a <a href="hhttps://sistemafirmadigitaldpsn.prefecturanaval.gob.ar/login">https://sistemafirmadigitaldpsn.prefecturanaval.gob.ar/login</a></p>';
    # }
}
